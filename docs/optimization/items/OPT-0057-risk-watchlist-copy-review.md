# Risk Watchlist（/risk-watchlist）文案审阅表

> 目的：把 `/risk-watchlist` 页面当前**所有注释文字 / tooltip / 说明文案**整理出来，供你逐条修改。
>
> 用法：每条有「**当前**」和「**改后 ✍️**」两块。你只需在 `改后 ✍️` 的代码块里填新文案（留空 = 保持不变）。填完把这份文件给我，我按编号回填进源码。
>
> 覆盖范围：页面实际渲染的是 **全量客户 · 交易状态视图**（`frontend/src/pages/ActivityClientsPanel.tsx`）。旧的「返佣观察清单」roster 视图已于 2026-07-24 退役、UI 不可达，故不在本表内（如需一并改，告诉我）。
>
> 说明：tooltip 里的 `\n` = 换行，多选下拉/横幅里的中文照原样。`—` 是缺数据占位符（em dash），不建议改动。

---

## A. 顶部说明横幅（页面最上方灰色说明框，3 段）

### A1 — 第 1 段：视图定义 + 客户范围
**当前**
```
全量客户 · 交易状态视图（服务端分页）  ·  一行 = 一个客户（userId，多账户合并）；客户范围由「CRM属性」下拉决定——勾选 = 该属性为真、未勾 = 为假，五个字段始终参与过滤（默认：已验证 ∧ 启用 ∧ 非 lead/员工/纯 demo）
```
**改后 ✍️**
```

```

### A2 — 第 2 段：交易状态口径
**当前**
```
交易状态 为互斥单值，优先级瀑布首个命中即停：持仓中 > 近7/30/90天 > 超90天未交易 > 入金未交易 > 新注册(30d)未入金 > 长期未入金。「交易」= 有平仓记录，只开仓未平过仓的新客由「持仓中」兜住；「入过金」按 总入金（不减出金）判定（净入金会误判入金后全提走的客户）。
```
**改后 ✍️**
```

```

### A3 — 第 3 段：数据新鲜度
**当前**
```
数据新鲜度：持仓/锁仓/品种列仅「持仓中」档有值（60 秒快照）；已平仓 PL 当日 ≤10 分钟入账；返佣 / 交易净入金 为 T-1；净值 / 浮动PL 每 30 秒；净赚 = 已平仓PL + 浮动PL + 全链返佣。排序仅支持生涯/注册类列（指标列跨全量排序第一期不做）。
```
**改后 ✍️**
```

```

---

## B. 工具栏（搜索框 / 按钮 / 下拉标题与说明）

### B1 — 搜索框 placeholder
**当前**
```
Userid / 姓名 / 国家
```
**改后 ✍️**
```

```

### B2 — 「交易状态」下拉 · 菜单标题
**当前**
```
交易状态
```
**改后 ✍️**
```

```

### B3 — 「国家」下拉 · 菜单标题
**当前**
```
国家
```
**改后 ✍️**
```

```

### B4 — 「CRM属性」下拉 · 菜单标题
**当前**
```
CRM属性
```
**改后 ✍️**
```

```

### B5 — 「CRM属性」下拉 · 顶部说明行（note）
**当前**
```
勾选 = 该属性为真,未勾 = 为假
```
**改后 ✍️**
```

```

### B6 — 「国家」下拉 · 全选行文字
**当前**
```
全选
```
**改后 ✍️**
```

```

---

## C. 交易状态 buckets（badge 标签 + 下拉选项，共 9 个 · 显示名，后端 code 不变）

> 这些同时出现在：状态列的 badge、工具栏「交易状态」下拉选项、以及横幅里的瀑布顺序。改一处这里就统一改到位。

| 编号 | code | **当前显示名** | **改后 ✍️** |
|---|---|---|---|
| C1 | `holding` | 持仓中 | |
| C2 | `active_1d` | 近1天 | |
| C3 | `active_7d` | 近7天 | |
| C4 | `active_30d` | 近30天 | |
| C5 | `active_90d` | 近90天 | |
| C6 | `dormant` | 超90天未交易 | |
| C7 | `funded_no_trade` | 入金未交易 | |
| C8 | `new_no_fund` | 新注册(30d)未入金 | |
| C9 | `no_fund` | 长期未入金 | |

---

## D. 国家下拉选项（显示名，后端 code 不变）

| 编号 | code | **当前显示名** | **改后 ✍️** |
|---|---|---|---|
| D1 | `CN` | CN | |
| D2 | `TH` | TH | |
| D3 | `VN` | VN | |
| D4 | `NG` | NG | |
| D5 | `LA` | LA | |
| D6 | `TW` | TW | |
| D7 | `OTHER` | 其他 | |

---

## E. CRM属性下拉选项（label + hint，后端 code 不变）

| 编号 | code | **当前 label** | **当前 hint（副说明）** | **改后 label ✍️** | **改后 hint ✍️** |
|---|---|---|---|---|---|
| E1 | `lead` | isLead | （无） | | |
| E2 | `verified` | isVerified | （无） | | |
| E3 | `enabled` | isEnabled | （无） | | |
| E4 | `employee` | isEmployee | （无） | | |
| E5 | `demo` | 纯Demo | 只有Demo账户无Live账户 | | |

---

## F. 「CRM Tags」下拉（组件 CrmTagsFilter）

### F1 — 下拉内说明行
**当前**
```
多选任一命中（OR）；空选 = 不过滤
```
**改后 ✍️**
```

```

### F2 — 搜索框 placeholder
**当前**
```
搜索 tag
```
**改后 ✍️**
```

```

### F3 — 字典加载失败提示
**当前**
```
标签字典加载中或加载失败——稍后再试
```
**改后 ✍️**
```

```

### F4 — 无匹配提示
**当前**
```
没有匹配的 tag
```
**改后 ✍️**
```

```

### F5 — 清空按钮
**当前**
```
清空 ({数量})
```
**改后 ✍️**
```

```

---

## G. 列头 ℹ️ tooltip（InfoHeader，鼠标悬停列头显示）

### G1 — 列「交易状态」
**当前**
```
互斥单值状态，优先级瀑布首个命中即停：持仓中 > 近1/7/30/90天 >
超90天未交易 > 入金未交易 > 新注册(30d)未入金 > 长期未入金。
「交易」= 有平仓记录；只开仓未平过仓的新客由「持仓中」兜住。
用工具栏「交易状态」多选下拉筛选状态（空选 = 不查询）。
```
**改后 ✍️**
```

```

### G2 — 列「Zipcode」
**当前**
```
CRM 账户 zipcode（客户名下合规账户去重；多个不一致时逗号并列）。
来源 fxbackoffice.mt4_users，非 KCM 快照。
```
**改后 ✍️**
```

```

### G3 — 列「CRM Tags」
**当前**
```
客户在 CRM 打的标签（颜色与 CRM 一致，来源 KCM 镜像，≤5 分钟延迟）。
超出部分收进 +N，点击单元格弹出全部标签（按分类分组）。
可用工具栏「CRM Tags」下拉按标签筛选（多选任一命中）。
≠ 案卷引擎的行为风控 tag——这里是 CRM 人工标注。
```
**改后 ✍️**
```

```

### G4 — 列「最近交易」
**当前**
```
最近一次平仓日（T13 生涯汇总，按 closeDate 聚合）。
从未平过仓的客户显示 —（含只开仓未平的「持仓中」新客）。
默认按此列降序，空值排最后。
```
**改后 ✍️**
```

```

### G5 — 列「生涯累计 · 总入金」
**当前**
```
总入金（Total Deposit）：生涯累计入金金额，只加入金、不减出金。
这是判定「入过金」的口径，
净入金会把入金后全提走的客户误判成未入金。
资金历史起点 2021-07-28，更早只入过金的客户可能显示 —。
展开列组可见生涯手数 / 订单数 / 交易天数 / 首笔交易。
```
**改后 ✍️**
```

```

### G6 — 列「锁仓 · 锁仓比例」
**当前**
```
同一品种内买/卖成对锁住的比例 = 2×对锁手数 ÷ 总持仓手数(后端已按 base_symbol 配对,跨品种反向敞口不计入)。
≥95% ≈ 净敞口趋零的真锁仓。展开列组可见对锁手数。
仅「持仓中」档有值。
```
**改后 ✍️**
```

```

### G7 — 列「浮动PL」
**当前**
```
账户级权威浮动盈亏 = EQUITY − BALANCE − CREDIT，客户名下全部账户求和；每 30 秒刷新。
净赚的浮动腿。缺数据显示 —（不当 0）。
```
**改后 ✍️**
```

```

### G8 — 列「净入金差额 · 交易净入金」
**当前**
```
stats_transactions type IN (deposit, withdrawal)，历史累计 USD。
与 IB 佣金提现拆开看（展开列组可见）；T-1，每日凌晨更新。
```
**改后 ✍️**
```

```

### G9 — 列「Total Profit · History」
**当前**
```
已平仓 Total Profit，生涯累计（History），不含浮动。
PL+Rebate 的 PL 腿。当日已平仓约 ≤10 分钟入账。
展开列组可见 30d / 7d 窗口。
```
**改后 ✍️**
```

```

### G10 — 列「总反佣 · History」
**当前**
```
全链返佣（含多级 wallet），生涯累计（History）。
PL+Rebate 的 Rebate 腿；与 CRM 单 IB 报表差异属预期口径。
T-1：当日返佣次日凌晨才入账。
```
**改后 ✍️**
```

```

### G11 — 列「已平仓 PL+Rebate · History」
**当前**
```
已平仓 Total Profit (History) + 全链返佣 (History)，生涯累计。
> 0 = 公司净流出（绿色 = 客户赢）。仅含已平仓盈亏，不含浮动。
```
**改后 ✍️**
```

```

### G12 — 列「净赚」
**当前**
```
净赚 = 已平仓PL + 浮动PL + 全链返佣（客户级）。
客户+代理链最终从公司赢走多少：> 0 客户赢、公司亏（绿色）；< 0 客户输、公司赚（红色）。
浮动腿为 30 秒刷新的账户级权威值；返佣腿 T-1（当日返佣次日入账）。
任一腿缺失显示 —（不假设为 0）。
```
**改后 ✍️**
```

```

### G13 — 列「净值」
**当前**
```
客户名下 live 交易账户(sid 1/5/6)净值 EQUITY = BALANCE + 浮动盈亏，逐账户求和；每 ~30 秒刷新快照。
只含真实交易账户——不含 IB 佣金钱包(sid=2，IB-WALLET-USD，装未提返佣)与 demo 账户(sid=3)。这两类在 KCM 底座就已排除。
有未平仓时净值逐 tick 变动，与 CRM 后台不同时刻拍的快照可能差几美元。
缺数据显示 —（不当 0）。
```
**改后 ✍️**
```

```

### G14 — 列「加权持仓时间 · 加权」
**当前**
```
已平仓算术加权平均每手持仓时长 = Σ(持仓秒×手数) ÷ Σ手数，30 天滑窗（MT server 日界）。重尾敏感——几列一起看。
自适应单位 34m / 5.2h / 2.1d；窗口内无平仓显示 —（不当 0）。
展开列组可见 ∆1 / ∆30 趋势。当日平仓约 ≤10 分钟入账。
```
**改后 ✍️**
```

```

### G15 — 列「加权持仓时间 · 加权∆1」
**当前**
```
加权现值 − as-of 昨日的 30d 窗口值。
负值红色 = 持仓变短 = 刷单化趋势；任一侧无数据显示 —。
```
**改后 ✍️**
```

```

### G16 — 列「加权持仓时间 · 加权∆30」
**当前**
```
加权现值 − as-of 30 天前的 30d 窗口值（需 60 天 T4 历史，已闭合）。
负值红色 = 持仓变短 = 刷单化趋势；任一侧无数据显示 —。
```
**改后 ✍️**
```

```

### G17 — 列「加权持仓时间 · 几何」
**当前**
```
已平仓几何加权均值 = EXP(Σ(ln持仓秒×手数) ÷ Σ手数)，30 天滑窗。
重尾稳健——个别长持仓拉不高它，盯刷单主力用这列。
窗口内无平仓显示 —（不当 0）。展开列组可见 ∆1 / ∆30。
```
**改后 ✍️**
```

```

### G18 — 列「加权持仓时间 · 几何∆1」
**当前**
```
几何现值 − as-of 昨日的 30d 窗口值。
负值红色 = 持仓变短 = 刷单化趋势；任一侧无数据显示 —。
```
**改后 ✍️**
```

```

### G19 — 列「加权持仓时间 · 几何∆30」
**当前**
```
几何现值 − as-of 30 天前的 30d 窗口值（需 60 天 T4 历史，已闭合）。
负值红色 = 持仓变短 = 刷单化趋势；任一侧无数据显示 —。
```
**改后 ✍️**
```

```

---

## H. 列头普通 tooltip（headerTooltip 字符串，默认 2 秒延迟、无 ℹ️ 图标）

> 注：这些列默认隐藏（需在「列」菜单里开启才能看到），H1–H5 是 CRM 布尔标记列。

### H1 — 列「已验证」（is_verified）
**当前**
```
CRM isVerified。verified 是事实上的交易前置（活跃交易客户 100% verified）。
```
**改后 ✍️**
```

```

### H2 — 列「启用」（is_enabled）
**当前**
```
CRM isEnabled。FALSE = 账户停用。
```
**改后 ✍️**
```

```

### H3 — 列「Lead」（is_lead）
**当前**
```
CRM isLead。TRUE = 未通过验证的潜在客户，不算真实客户。「CRM属性」下拉按勾选值过滤（默认未勾 = 只看 FALSE）。
```
**改后 ✍️**
```

```

### H4 — 列「纯Demo」（is_all_demo）
**当前**
```
有 MT 账户且全部为 demo 账户 ⇒ TRUE（MT 推导，非 CRM 字段）。「CRM属性」下拉按勾选值过滤（默认未勾 = 只看 FALSE）。
```
**改后 ✍️**
```

```

### H5 — 列「员工」（is_employee）
**当前**
```
CRM isEmployee。TRUE = 员工/内部账号。「CRM属性」下拉按勾选值过滤（默认未勾 = 只看 FALSE；勾上 = 只看员工）。
```
**改后 ✍️**
```

```

### H6 — 列「账户数」（account_count）
**当前**
```
当前有未平仓持仓的账户数（仅「持仓中」档有值）。
```
**改后 ✍️**
```

```

### H7 — 列「锁仓 · 对锁手数」（hedged_lots）
**当前**
```
同一品种内买/卖成对锁住的手数(各品种取较小边求和,单边口径)。用于判断锁仓比例背后的实际体量。
```
**改后 ✍️**
```

```

### H8 — 列「最长持仓」（longest_hold）
**当前**
```
当前时间 − 最早一笔未平仓开仓时间（实时计算，仅「持仓中」档有值）。
```
**改后 ✍️**
```

```

### H9 — 列「品种」（symbols）
**当前**
```
当前未平仓品种（仅「持仓中」档有值）。
```
**改后 ✍️**
```

```

---

## I. 状态栏 / 空状态 / 提示文案

### I1 — 空选提示：状态与国家都未选
**当前**
```
未选择交易状态与国家——请在两个下拉中至少各勾选一项
```
**改后 ✍️**
```

```

### I2 — 空选提示：仅状态未选
**当前**
```
未选择交易状态——请在下拉中至少勾选一个状态
```
**改后 ✍️**
```

```

### I3 — 空选提示：仅国家未选
**当前**
```
未选择国家——请在下拉中至少勾选一个国家
```
**改后 ✍️**
```

```

### I4 — 数据源不可用错误
**当前**
```
数据源暂不可用，请稍后重试
```
**改后 ✍️**
```

```

### I5 — 右上角统计条（状态摘要 + 总数）
**当前**（`{摘要}` = 「近7天」/「N 个状态」/「未选状态」；`{数字}` 动态）
```
{摘要} · 共 {数字} 位客户
```
**改后 ✍️**
```

```

### I6 — 右上角快照时间徽标（仅选中「持仓中」时出现）
**当前**（`{时间}` 动态）
```
快照 {时间} (HK)
```
**改后 ✍️**
```

```

---

## J. 列名（列头显示名，供参考；如需一并改在此填）

| 编号 | colId | **当前列名** | **改后 ✍️** |
|---|---|---|---|
| J1 | user_id | Userid | |
| J2 | activity_status | 交易状态 | |
| J3 | user_name | 客户名 | |
| J4 | country | 国家 | |
| J5 | zipcode | Zipcode | |
| J6 | crm_tags | CRM Tags | |
| J7 | last_trade_date | 最近交易 | |
| J8 | registered_at | 注册时间 | |
| J9 | grp_lifetime | 生涯累计（组） | |
| J10 | lifetime_deposit | 总入金 | |
| J11 | lifetime_lots | 手数 | |
| J12 | lifetime_orders | 订单数 | |
| J13 | trade_days | 交易天数 | |
| J14 | first_trade_date | 首笔交易 | |
| J15 | is_verified | 已验证 | |
| J16 | is_enabled | 启用 | |
| J17 | is_lead | Lead | |
| J18 | is_all_demo | 纯Demo | |
| J19 | is_employee | 员工 | |
| J20 | position_count | 持仓单数 | |
| J21 | account_count | 账户数 | |
| J22 | grp_lots | 持仓手数（组） | |
| J23 | total_lots | 总手数 | |
| J24 | buy_lots | 买手数 | |
| J25 | sell_lots | 卖手数 | |
| J26 | grp_hedge | 锁仓（组） | |
| J27 | hedge | 锁仓比例 | |
| J28 | hedged_lots | 对锁手数 | |
| J29 | floating_pl | 浮动PL | |
| J30 | grp_nd | 净入金差额（组） | |
| J31 | trading_net_deposit | 交易净入金 | |
| J32 | ib_withdrawal | IB佣金提现 | |
| J33 | grp_profit | Total Profit（组） | |
| J34 | profit_all | History | |
| J35 | grp_rebate | 总反佣（组） | |
| J36 | grp_combined | 已平仓 PL+Rebate（组） | |
| J37 | net_gain | 净赚 | |
| J38 | equity | 净值 | |
| J39 | grp_closed_hold | 加权持仓时间（组） | |
| J40 | closed_avg_hold_sec_30d | 加权 | |
| J41 | closed_geo_hold_sec_30d | 几何 | |
| J42 | longest_hold | 最长持仓 | |
| J43 | symbols | 品种 | |

---

_共计：横幅 3 段（A）· 工具栏 6（B）· 状态 9（C）· 国家 7（D）· CRM属性 5（E）· Tags 下拉 5（F）· ℹ️ tooltip 19（G）· 普通 tooltip 9（H）· 提示 6（I）· 列名 43（J）。_
