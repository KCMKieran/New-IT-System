version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: new-it-frontend-dev
    working_dir: /app
    command: sh -lc "[ -d node_modules ] || npm ci; npm run dev -- --host 0.0.0.0 --port 5173"
    ports:
      - "127.0.0.1:5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
      - WATCHPACK_POLLING=true
      - VITE_API_PROXY_TARGET=http://host.docker.internal:8001
      - VITE_DISABLE_AUTH=true
      # Dev uses direct IP access, no need for domain-based HMR
      # HMR works automatically at http://10.6.20.138:5173
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"


